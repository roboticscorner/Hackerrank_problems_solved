#include <iostream>

using namespace std;

int main() {
    int a, b, c;
    
    // Read three integers from stdin
    cin >> a >> b >> c;
    
    // Calculate the sum of the three integers
    int sum = a + b + c;
    
    // Print the sum to stdout
    cout << sum << endl;
    
    return 0;
}
